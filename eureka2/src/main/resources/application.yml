eureka:
    instance:
        hostname: localhost
    client:
        serviceUrl:
            defaultZone: http://${eureka.instance.hostname}:8761/eureka/
        register-with-eureka: true
        fetch-registry: true
server:
    port: 9100
spring:
    application:
        name: eureka